# Box

Box is a generic components that pass most of the `styled-system` props in to the component.
It's useful for when we need a flexible styled component

## Example

<Box width={250} bg="teal" color="white" p={2}>
  The fox on the box
</Box>
