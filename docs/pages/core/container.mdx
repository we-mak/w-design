# Container

Container contains Flexbox components to form the responsive layout.
`Container` uses `color, space, display, maxWidth, minWidth`
Api properties from styled-system. [See more here](https://styled-system.com/api)

## Example

<Container bg="N2">Full width container</Container>

```jsx
<Container bg="N2">Full width container</Container>
```
