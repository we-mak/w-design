import * as React from "react";
import { Card, CardContent, Container } from "w-design";

export const HooksExample = () => {
  return (
    <Container maxWidth={500}>
      <Card>
        <CardContent>Form</CardContent>
      </Card>
    </Container>
  );
};
