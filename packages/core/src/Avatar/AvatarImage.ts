import styled from "styled-components";
import Image from "../Image";

export const AvatarImage = styled(Image)`
  border-radius: 50%;
  height: 100%;
  width: 100%;
  position: relative;
  z-index: 1;
`;
