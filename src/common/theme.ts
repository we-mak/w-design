import { colors } from "./colors";

export const fonts = {
  0: "system-ui, sans-serif",
  sans:
    '-apple-system, BlinkMacSystemFont, Roboto, "Helvetica Neue", sans-serif, system-ui, sans-serif'
};

export default {
  fonts,
  colors
};
